#!/bin/sh
set -eu

HOST_PATH="${1:-}"
if [ -z "$HOST_PATH" ]; then
    echo "missing host path" >&2
    exit 127
fi

RUNNER="${PJARCZAK_MAC_LINUX_GUEST_RUNNER:-}"
if [ -z "$RUNNER" ]; then
    echo "PJARCZAK_MAC_LINUX_GUEST_RUNNER not set" >&2
    exit 127
fi

exec "$RUNNER" "$HOST_PATH"
